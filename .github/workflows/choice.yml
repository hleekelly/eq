name: worker

on:
  push:

jobs:
  stereotype:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        display: ["groundlessly", "complex", "preliminary", "hobby", "prescription"]
        strange: ["regularly", "trail", "burn", "onto"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar di
